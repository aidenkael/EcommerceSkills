# 构建与验证报告

- 版本：1.0.0
- 校准来源：用户上传的 `3-AI头程含说明-链接 .xlsx`
- 已导入校准记录：79 条
- 已提取校准图片：79 张
- 当前货代规则：
  - 深圳：80 元/kg + 10 元固定服务费
  - 义乌：100 元/kg + 6 元固定服务费
- 旧“包类/非包类单价”规则：已停用，仅作为历史相似标签

## 已执行验证

1. Python 全模块编译检查通过。
2. 5 项自动化测试全部通过。
3. 校准图片精确匹配、文字相似匹配通过。
4. 新增反馈写入并重新参与匹配通过。
5. HTTP API `/health` 通过。
6. HTTP API `/v1/estimate` 结构化核算通过。
7. Agent JSONL 批量核算及 CSV/JSONL 输出通过。

## 设计边界

- 默认只计算头程；尾程必须显式传入。
- 图片未标注重量/尺寸时，不伪造精确数字，而是使用校准推断并输出区间。
- 独立自动识图需要视觉 API 或本地 Ollama；直接交给能看图的 Agent 时无需额外 API。
