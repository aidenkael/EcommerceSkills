# AGENTS.md — EcommerceSkills 仓库根规则

> 详细工作流规范见 `docs/AGENT_WORKFLOW.md`，本文件只保留强制底线。

## 仓库边界

- `E:\EcommerceSkills` 是唯一仓库根目录。
- 每个一级子文件夹是独立项目，不得改名、移动或混合文件。
- 修改前确认所属项目，只改任务相关文件。
- 子项目 `AGENTS.md` 只写该项目特有业务规则。

## Git 与数据安全底线

- 每个 Commit 单一、清晰、可回退。
- 不提交缓存、日志、真实数据库、密钥、Token、Cookie、临时输出、压缩包。
- 不强制推送，不覆盖 Git 历史。
- 推送前检查 `git diff` 和敏感信息。

## 测试底线

- 禁止删除测试、降低断言、把失败改成跳过或伪造通过。
- 高风险修改（利润、物流、汇率、数据库迁移）须独立分支 + 全量回归。

## 报告脚本

每个有效步骤完成后运行：

```bash
python tools/generate_step_report.py
```

Agent 只补充实现内容、关键设计、真实风险和遗留问题。

## 完成反馈格式

```text
完成内容：
关键文件：
测试结果：
报告路径：
Commit：
推送状态：
真实风险/遗留问题：
```
