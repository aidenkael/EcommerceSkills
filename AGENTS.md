# AGENTS.md — EcommerceSkills 仓库通用规则

## 仓库结构

- `E:\EcommerceSkills` 是唯一仓库根目录。
- 每个一级子文件夹是独立项目，不得随意改名、移动或混合文件。
- 修改前先确认本次任务属于哪个项目，只修改任务相关文件。
- 子项目中的 `AGENTS.md` 只补充该项目特有规则，不重复本文内容。

### 项目最高需求文件

- **`Profit accounting-Auto`（微智能利润管理软件）**：涉及该项目的任何开发、需求讨论、UI 调整、物流移植、AI 接入、数据设计、测试、打包和维护任务，**必须先读取** `Profit accounting-Auto/docs/Development rules-1.5.md` 作为最高需求依据。该文件优先级高于旧报告、旧代码、旧测试和历史讨论。

## 分步骤开发与记录

每个任务拆为若干可独立验证的步骤。**步骤**指有实际代码/配置变化的阶段；
纯分析、失败尝试、查看文件等计入当前步骤说明，不单独提交。

每完成一个有效步骤，生成一份步骤说明：

```
review_packages/<项目名称>/<任务名称>/step-NN_步骤名称.md
```

每份步骤说明包含：目标、修改文件、具体修改、测试/验证、当前结果、未解决问题、下一步。

报告通过文件路径和 Git diff 引用代码，不复制整段代码。

任务全部完成后，在同目录生成 `final_report.md`，汇总：完成情况、全部修改、关键逻辑变化、测试结果、已知问题、复审重点、最终 commit。

禁止把报告散落在仓库根目录或项目源码目录。

## Git 提交

- 每个有效步骤完成后，代码 + 对应步骤说明一起提交。
- 一个 commit 只含一个明确步骤或紧密相关的小修改。
- 提交信息格式：`<项目名称>：<步骤简述>`
- 推送前检查 `git diff`、测试结果、敏感信息。
- 不提交缓存、日志、临时文件、压缩包、账号、Token、Cookie、真实数据库。
- 禁止强制推送和覆盖 Git 历史。

## 完成反馈

每次完成后向用户报告：所属项目、当前步骤、修改文件、测试结果、报告路径、分支、commit、推送状态。

## 开发工具与流程

详见 `docs/AGENT_WORKFLOW.md`。核心要求：

- 中等粒度开发，纯分析不计入独立步骤；
- 按风险决定测试强度（核心计算/迁移全量回归，UI/工具可先局部后全量）；
- 每个有效步骤完成后运行 `tools/run_step_report.bat <phase>` 生成报告；
- 聊天中只输出摘要和报告路径，不贴大段代码或报告正文。
