# START_HERE_FOR_AGENT.md

这是物流 2.0 Agent 工具根目录。

## Agent 首次读取

1. 先检查根目录完整性：`bash extract_and_verify.sh`。
2. 这是物流 2.0 工具根目录，包含头程校准和完整物流利润核算功能。
3. 先读取根目录 `AGENTS.md`。
4. 再读取 `workflows/SESSION_ROUTER.md`。
5. 先运行环境检查：`python3 tools/mac_bootstrap.py`。
6. 启动时只向用户显示简短模式选择。
7. 不允许依赖对话记忆代替本地规则。
8. 不允许混用头程校准和完整利润模式。
9. 用户上传图片后，按现有工作流自动处理。

## 首次输出

物流2.0工具已读取。

请选择本对话用途：
1. 仅校准头程
2. 完整物流与利润核算

本对话选定后不混用。回复1或2。
