# Step-03：设置与商品界面

目标：在设置中维护规则，在商品页默认选择“无”并只显示启用且未归档规则。

修改：`ui/main_window.py`、`ui/product_page.py`、`config/config_manager.py`。

修改内容：设置窗口增加规则管理对话框（新增、编辑保存、启用、归档/删除、恢复）；商品页新增单选下拉与实时说明。刷新操作沿用 Phase 1.5 未保存修改保护。

验证：既有设置保护测试保持通过；未执行真实 GUI。

下一步：冻结快照与完整回归测试。
