# Step-02：利润计算接入

目标：将单条明确选择的规则转换为 RMB 后计入净利润。

修改：`calculation/profit_adjustments.py`、`ui/product_page.py`。

修改内容：支持受限条件字段、五种比较、固定/百分比、收入/成本、USD/RMB；百分比必须显式保存 RMB 计算基数。

验证：28.99 触发、29.00/29.01 不触发、缺少售价不触发、RMB/USD 固定金额、百分比和成本扣减均有测试。

结果：不支持表达式、多规则叠加或自动选择。

下一步：接入设置与历史快照。
