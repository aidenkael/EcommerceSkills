# 审查重点 — phase_01_fix_01

## 本次修复最需要检查的内容

### 1. P0-1 默认规则是否正确
- **检查**：`database/db_manager.py` DEFAULT_CONFIG 中 `fixed_service_fee="6.0"`, `default_tail_haul="40.0"`
- **检查**：`config/config_manager.py` 属性默认值也同步为 6.0/40.0
- **检查**：测试 `test_config_defaults` 期望值正确

### 2. P0-2 缺失费用传播
- **手动测试**：填写成本和售价，不填包装尺寸 → 利润应显示"数据不足(物流费用不完整)"
- **手动测试**：填写全套数据 → 利润应显示确定数字

### 3. P0-3 推广比例联动
- **手动测试**：填成本100、目标利润率30% → 看到建议售价 → 再填推广比例10% → 建议售价应更新（不是清空）

### 4. P0-4 售价不被覆盖
- **手动测试**：填售价200 → 修改目标利润率 → 售价输入框仍为200
- **手动测试**：不填售价 → 修改目标利润率 → "建议售价"标签更新，但售价输入框为空

### 5. P0-5 历史商品保护
- **手动测试**：保存商品（头程100） → 修改头程单价为120 → 重新打开商品 → 头程仍显示100
- **手动测试**：费率变更后应显示橙色提示条
- **手动测试**：点击「用当前费率重算」→ 头程变为120

### 6. P0-6 清空不覆盖
- **手动测试**：打开历史商品 → 清空 → 保存 → 数据库应新增一条记录
- **检查**：`clear_form()` 第3行 `self._product_id = None`

### 7. P0-7 迁移机制
- **检查**：`schema_version` 表存在且版本=1
- **检查**：`_add_column_if_missing()` 方法使用 try/except 安全处理已有列

### 8. P1 界面改进
- **手动测试**：设置对话框头程输入 -1 → 应报错
- **手动测试**：历史列表利润率列显示的是 `(售价-成本)/售价` 而非目标利润率
- **手动测试**：保存商品后切换到历史标签 → 列表自动刷新

### 9. 测试
- `python -m pytest tests/ -v` → 61 passed
- 特别检查 `test_config_migration` 是否通过

## 无需检查的内容

以下在修复中未改变，如已在原审查中通过则无需重新检查：
- 体积重公式（未改）
- 利润/利润率计算（未改）
- 人民币↔美元换算（未改）
- 其他纯函数测试（未改）
