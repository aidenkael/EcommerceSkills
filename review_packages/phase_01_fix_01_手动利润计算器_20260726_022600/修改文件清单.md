# 修改文件清单 — phase_01_fix_01

## 本次修复涉及的文件（7个）

### 1. `database/db_manager.py` — 重写
- **DEFAULT_CONFIG**：`fixed_service_fee` 36→6, `default_tail_haul` 0→40
- **SCHEMA_SQL**：新增 `schema_version` 表；`product_snapshots` 增加 `exchange_rate`, `head_haul_rate`, `fixed_service_fee`, `rule_version` 列
- **_add_column_if_missing()**：安全补列（已有列跳过）
- **_migrate_config()**：检测旧出厂默认值 36/0 并自动更新为 6/40
- **save_snapshot()**：新增 `rules` 参数，保存当时的汇率和费率
- **get_snapshot()**：返回 `_snapshot_exchange_rate` 等费率字段

### 2. `config/config_manager.py` — 修改
- `fixed_service_fee` 属性默认值 36.0 → 6.0
- `default_tail_haul` 属性默认值 0.0 → 40.0

### 3. `ui/product_page.py` — 重写（最大改动）
- **P0-2**：新增 `_partial` 字典标记、`_set_result` 支持 partial 显示、"数据不足(物流费用不完整)"
- **P0-3**：用 `_calc_direction` 替代 `_last_modified` 判断计算方向；`promo_rate` 不改变方向
- **P0-4**：`_calc_direction == "rate"` 时不再 `_var_price_rmb.set(...)`
- **P0-5**：新增 `_historical_mode`、`_check_rate_changes()`、`_show_rate_notice()`、「用当前费率重算」按钮
- **P0-6**：`clear_form()` 首行增加 `self._product_id = None`

### 4. `ui/main_window.py` — 修改
- **SettingsDialog._save()**：增加 head/fixed/tail 的负数校验
- **_on_tab_changed()**：切换到历史标签时调用 `_history_page.refresh_list()`

### 5. `ui/history_page.py` — 重写
- 列名：`成本(元)` → `采购成本(元)`，`利润率(%)` → `实际利润率(%)`
- 利润率列计算方式：`(售价-成本)/售价*100`（替代原 `target_profit_rate`）

### 6. `tests/test_database.py` — 更新
- 期望值从 36/0 改为 6/40
- 新增 `test_schema_version_exists`
- 新增 `test_config_migration`
- `test_snapshot` → `test_snapshot_with_rules`（验证规则字段）

### 7. `data/profit_accounting.db` — 重建
- 旧数据库已删除，重新生成（含正确默认值和新表结构）

## 未修改的文件
- `calculation/logistics.py` — 纯函数逻辑正确，无需修改
- `calculation/profit.py` — 纯函数逻辑正确，无需修改
- `calculation/currency.py` — 无需修改
- `app.py` — 入口不变
- `tests/test_logistics.py` — 无需修改
- `tests/test_profit.py` — 无需修改
- `tests/test_currency.py` — 无需修改
